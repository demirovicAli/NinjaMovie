angular.module('ninja.user').controller('userCtrl', function($rootScope, $scope,API_KEY, userService) {

/*    $rootScope.token = '';

    $rootScope.getToken = function() {
        userService.getToken().then(function(response) {
                $rootScope.token = response;
                console.log($rootScope.film);
            }

        );
    };*/
});
